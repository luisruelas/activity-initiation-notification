version: '3.8'

services:
  app:
    platform: linux/arm64
    build: .
    image: vivanta-whoop-auth
    container_name: vivanta-whoop-auth
    volumes:
      - .:/var/task
    env_file:
      - .env
    ports:
      - "9001:8080"
    networks:
      - lambda_postgres_network

networks:
  lambda_postgres_network:
    driver: bridge
